package com.killian;

/**
 * Enum object to handle which stage of the game is currently in place. 
 */
public enum Stage {
    INKITCHEN,
    INHAND,
    PLAYER,
    MOVEMENT,
    CALCULATE_RULES,
    GAME_OVER
}